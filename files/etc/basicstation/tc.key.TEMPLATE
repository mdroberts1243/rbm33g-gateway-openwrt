# DO NOT COMMIT REAL KEYS IN GIT.
# TTN LNS Authentication Key goes here.
# Format must be exactly:
#   Authorization: Bearer <LNS_KEY>\r\n
# Note: Must end with CRLF (carriage return + newline).
Authorization: Bearer REPLACE_WITH_REAL_LNS_KEY
